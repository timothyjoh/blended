Added a Known Limitations entry to README.md flagging the two sharp edges this cycle introduced — `ResourcePane`'s sandboxed iframe has no blocked-embed fallback so a frame-denying URL renders as a broken/blank pane (`embedMode` stays `'blocked'`, not yet consulted), and the resource-activation dual-write, live pane sync, and Activate control are gated behind the admin-env-only `e2e/activate-resource.spec.ts` with the pure builder/fold unit-covered but the live `activateResource` write and cycle-0003 owner-only backstop lacking a runnable gate. No edits were needed to AGENTS.md, README.md's feature section, or release-notes.md — the build's Task 8 already added complete, review-verified cycle-0016 coverage there — and docs/release-notes.md (a stale theme-picker note) and ADR-0002 (future URL-stepping, out of scope here) were left untouched.
