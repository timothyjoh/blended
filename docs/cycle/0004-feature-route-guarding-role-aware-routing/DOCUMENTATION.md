Added a "Known limitations" bullet to README.md noting that route guarding is client-side only (SSR shell hydrates then decides, no server/middleware protection, content gated only after `useAuth` resolves) — the one product-doc gap remaining after the cycle's own build pass had already synced the README "Protected routes" section, the AGENTS.md "Route guarding" note, and the root release-notes.md "Cycle 0004" entry against the diff (`RouteGuard`/`SessionRouteGuard`/`safeNextPath`/`authorizeOwnership`, the placeholder `/dashboard` and `/dashboard/sessions/[id]` routes, open-redirect safety, and the reused testids); no other docs (`docs/SPEC.md`, `docs/adr/*`, the unrelated stale `docs/release-notes.md`) were drifted by this change.
