Added a deploy-prerequisite caveat to the `Data Layer & Event Spine` section of README.md, noting that the schema must be pushed with `npx instant-cli push schema` before deploying against a schema-enforcing Instant app or `writeEvent()` transactions are rejected — surfacing the medium-priority reflection sharp edge in the user-facing entry point (AGENTS.md already carried this note, and its Data Layer / Testing / Environment sections plus the README Commands table were already synced to `src/lib/db.ts`, the `writeEvent()` dual-write convention, and the Vitest/Playwright gates by the build step, so no further edits were warranted there). CONTEXT.md domain language and docs/adr/* remain accurate for this cycle and were left untouched.
