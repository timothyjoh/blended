Appended a brief deferred-trade-off caveat to the cycle-0012 note in AGENTS.md flagging that `sessions.teacherId` is un-indexed (`src/lib/db.ts`) so the new owner-scoped dashboard live query scans rather than seeks — fine at MVP scale but a tracked future-index follow-up now that the dashboard is a per-teacher hot path (surfacing the low-priority `refl-0012-sessions-teacherid-unindexed` reflection finding). No README.md edit was needed: the user-facing "Your sessions on the dashboard" section already documents the live owner-scoped list, click-through to facilitation, realtime updates, scoping, and the loading/empty/error states, and the comparator-duplication reflection finding is already acknowledged inline in the same AGENTS.md note ("the inline copies in `SessionLifecycle`/`StudentChat` are intentionally untouched").
