Updated README.md "Known limitations" with a caveat capturing the cycle-0010 reflection sharp edge — a failed teacher-queue `questions` live query renders as the empty state and is indistinguishable from a genuinely empty queue (logged only, not surfaced via the `role="alert"` path that covers answer-write rejection). The product docs already shipped in sync with the code this cycle (AGENTS.md Data Layer note on `answerQuestion`/`buildQuestionAnswer`, the `QuestionAnswered` fold, the teacher queue and testids; README "Triaging questions (teacher queue)" section with the cycle-0009 cross-reference updated; release-notes.md cycle 0010 entry), so no further edits were warranted there.
