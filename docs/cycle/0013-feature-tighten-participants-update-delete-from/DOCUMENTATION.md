Added the deny-by-default permission posture to the canonical "Permission rules" section of README.md — the SPEC's Documentation Updates required noting the new posture where a security/permissions section exists, but the build/fix steps had only updated the chat-messages known-limitation note further down; the section now states that the global `$default` denies every op, that any unruled entity (including future schema entities) is non-readable and non-writable, and that the `perms.test.ts` structural guard fails loudly otherwise. No other product docs needed changes: AGENTS.md's data-layer note and stale cycle-0008/0009/0010 references, plus README's chat-messages limitation, were already brought in sync this cycle, and the reflection's deferred question-tightening sharp edge is already captured as the Batch-2 follow-up in both README.md and AGENTS.md.
