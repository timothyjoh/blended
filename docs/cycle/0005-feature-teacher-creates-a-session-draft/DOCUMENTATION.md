Added a session-creation **Known limitations** entry to README.md surfacing the medium-priority REFLECTION finding that the dual-write/observability path is exercised only by `e2e/create-session.spec.ts`, which skips without `INSTANT_ADMIN_TOKEN`/`PUBLIC_INSTANTDB_APP_ID` and can therefore pass CI as a false green; added a second README Known-limitations bullet recording the low-priority `generateJoinCode` modulo bias (byte-mod-31 favors indices 0–7) and its rejection-sampling fix. No edits needed to AGENTS.md or release-notes.md (BUILD already documented the `createSession`/`writeEvent` dual-write path, `generateJoinCode`, `NewSession`, the reused testids, and the `queryAdmin` helper accurately) or to docs/adr/* (ADR-0001's dual-write decision is unchanged); docs/release-notes.md is an unrelated prior-feature artifact and was left untouched.
