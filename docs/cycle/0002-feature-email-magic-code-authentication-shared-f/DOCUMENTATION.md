Added a "Known limitations" subsection to the README sign-in section capturing the two REFLECTION sharp edges as caveats — the `useAuth` integration path has no runnable verification gate until `INSTANT_ADMIN_TOKEN` plus a live schema are provisioned (so the auth e2e suite stays skipped), and first-sign-in `users`-row creation silently skips (fail-safe, no log) on a projection-query error, so downstream reads must not assume the row always exists. AGENTS.md, `.env.example`, and release notes were already brought in sync by the build and needed no further edits.
