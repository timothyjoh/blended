Updated the stale "activated resource that refuses to be framed renders as a broken pane" bullet in README.md's Known limitations section — cycle 0018 resolved that gap, so it now documents the residual best-effort-detection caveat (a slow-but-valid embed can show the `EMBED_LOAD_TIMEOUT_MS`-triggered fallback card, which stays until `activeResourceId`/`currentUrlVersion` changes since the iframe is unmounted) plus the explicitly-deferred follow-ups (server-side preflight, a `failed`→retry affordance, auto-re-embedding). The build/fix steps had already added the user-facing "Blocked-embed fallback" section to README.md and the cycle-0018 data-layer paragraph to AGENTS.md, and corrected the over-promising late-`onLoad` comment flagged in REVIEW.md/REFLECTION.md, so no further edits were warranted there.
