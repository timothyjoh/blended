Updated README.md with a "Not yet live" caveat in the Permission rules section noting the rules are inert until an operator runs `push schema` + `npm run perms:push` and the live e2e gate passes (REFLECTION critical sharp edge). Added a fail-open warning to the `participants` bullet of the data-layer authorization section in AGENTS.md, flagging that `participants` update/delete currently allow any signed-in user and must be owner-restricted by the join-via-link cycle (REFLECTION high sharp edge). The cycle's own build had already synced the bulk of the permission-rules documentation (the `perms:push` command row and Permission rules section in README.md, and the full data-layer authorization subsection in AGENTS.md), so no further drift remained.
