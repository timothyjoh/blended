## Summary

Addressed all four MUST-FIX tasks for cycle 0003. **Task 2 (Critical — `sessionResources` create-time ownership hole)** is fixed via option (a): added an InstantDB link `sessionResourceSession` in `src/lib/db.ts` (forward `session` has-one on `sessionResources`, reverse `resources` has-many on `sessions`) and rewrote the `sessionResources` rule in `src/lib/perms.ts` so create/update/delete bind `isSessionOwner = "auth.id in data.ref('session.teacherId')"` instead of the forgeable self-asserted `auth.id == data.teacherId`. Ownership is now checked against the real linked parent session, which the client cannot forge, so a student can no longer inject a resource into a foreign teacher's lesson by declaring their own id as `teacherId`. The `perms.test.ts` structural guard was tightened to assert the bind uses `data.ref('session.teacherId')` and that no write op contains the bare `data.teacherId` self-assertion — this is the runnable regression reproduction at the rule level. **Task 3 (Minor — missing injection test)** is fixed: added an `injectResource()` probe + `probe-inject-resource` button in `PermsProbe.tsx` that exercises the dangerous `teacherId = own id` / `session = victim` vector, set the `session` link on the existing `writeResource()` probe, and added the matching `error:` denial assertion in `e2e/permissions.spec.ts`. **Task 4 (Minor — `participants` fail-open)** required no code change per its own instructions; it is recorded as a Batch-2 follow-up. **Task 1 (Critical — live push + live e2e) could not be fixed**: it is an operator step against shared live infrastructure requiring interactive `instant-cli login`, and this environment has no `PUBLIC_INSTANTDB_APP_ID`, no `INSTANT_ADMIN_TOKEN`, and no `instant-cli` auth config; `npx instant-cli push schema` / `npm run perms:push` and the live `e2e/permissions.spec.ts` (3 skipped loudly without the token) are not runnable here, so BUILD.md's "deferred" caveat is retained rather than removed. The Task 2 rule delta lands so that enforcement (including the closed injection hole) is correct once the operator runs the push.

Final test outcome: `npm run test` → **50 passed (4 files)**; `npm run astro check` → **0 errors, 0 warnings, 33 hints** (pre-existing `ElementRef` deprecations in unrelated `ui/` components); `npm run test:e2e -- e2e/permissions.spec.ts` → **3 skipped** (loud gate, no admin token). Coverage (`npm run test:coverage`, v8): **lines 66.66%, branches 61.29%, functions 64.7%** — identical to BUILD.md's baseline (66.66 / 61.29 / 64.7), no decrease; `db.ts` holds at 89.47% lines / 69.49% branch / 100% func (the added schema link is pure declaration evaluated at import), and the pure-string rule change in `perms.ts` stays fully covered by the structural guard. The only task left unresolved is Task 1, which is an external operator action and not actionable in this build environment.
