Added a README.md "Known limitations" bullet warning that chat `messages` ship under the permissive `$default` permission rule (open read/create/update/delete, spoofable `participantId`) with the participant/owner-scoped tightening to apply via the `messageSession` link before later cycles build on `messages`, and sharpened the AGENTS.md cycle-0008 "Student chat" note so its `$default` mention spells out the actual write/delete exposure (not just read-visibility) and the concrete scoping follow-up. No other product docs were stale — README's and AGENTS' cycle-0008 chat narratives, the dual-write/idempotency contract, schema-push step, and testids already matched the shipped `submitChatMessage`/`buildChatMessage` path, `ChatMessageSubmitted` fold, and `StudentChat` island.
