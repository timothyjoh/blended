## Summary

Addressed the single Critical item in MUST-FIX.md (Task 1) in `src/components/StudentChat.tsx`. Derived `const queryError = sessionQ.error || partsQ.error || messagesQ.error` (line 74) immediately after the three existing `console.error` calls — which were left intact — and wired it into the existing `student-chat-error` alert so the block now renders on `error || queryError` with a non-blank fallback (`'Chat is temporarily unavailable — please retry.'`) and retains `role="alert"` (lines 160–164); a failed `db.useQuery` load is now observable inline rather than presenting as a silently empty stream. Also closed the silent-drop path in `onSubmit`: the blank-only feedback branch was replaced so a non-blank submit that is gate-rejected for a non-duplicate reason (`existingForActionId === 0`, e.g. stream not yet loaded) now sets a visible `'Chat isn’t ready yet — please retry.'` message before returning, while a genuine duplicate (`existingForActionId > 0`) remains a correct silent storage no-op (lines 104–111). No error logging or propagation was weakened.

Verify checks all pass: `npm run astro check` → 0 errors, 0 warnings (35 pre-existing library hints); `grep -n "queryError" src/components/StudentChat.tsx` returns the new surfacing wiring at lines 74 and 160. Full unit suite: `npm test` → **193 passed** (matches BUILD.md). Coverage (`npm run test:coverage`): Statements **83.62%**, Branches **77.96%**, Functions **75%**, Lines **85.2%** — identical to BUILD.md's baseline (83.62 / 77.96 / 75 / 85.2), no metric decreased. The fix lives in a `.tsx` island, which the vitest config deliberately excludes from unit-coverage scope (`include: ['src/lib/**/*.ts']`); these islands are exercised by the Playwright suite (`e2e/student-chat.spec.ts`), so no lib-scope coverage shifted and no unit test was warranted by the change. No tasks were left unfixed.
