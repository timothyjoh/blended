Refreshed the bootstrap consequence in `docs/adr/0003-global-admin-role-and-internal-observability.md` from the tentative "via an env allowlist of admin emails (For Review)" to the shipped reality — the server-only `ADMIN_EMAILS` allowlist, the server-side-only `adminLevel: 'uber'` elevation via `@instantdb/admin` in `POST /api/admin/bootstrap`, the tightened client `users` rule that makes admin status unforgeable, and the replayable `AdminBootstrapped` event. The cycle's user- and contributor-facing surfaces were already in sync — `README.md` carries the new "Admin access (the `/admin` route + `ADMIN_EMAILS` allowlist)" section, `AGENTS.md` documents the uber-admin bootstrap step plus the server-only `ADMIN_EMAILS` key, `.env.example` describes `ADMIN_EMAILS`, and `CONTEXT.md` already defines the Uber Admin term — so no further edits were warranted there; `docs/SPEC.md` is intentionally left untouched since the admin role is an ADR-0003 addition outside the product spec's scope.
