Corrected one stale assertion in AGENTS.md: the cycle-0009 "Message → Question promotion" paragraph still claimed `messages` currently carries an open `allow: { $default: 'true' }` block, so its parenthetical now records that `messages` was tightened in cycle 0014 to participant-scoped create + row-owner/owning-teacher update/delete while `questions` stays the open Batch-2 follow-up. No other product-doc edits were warranted — the AGENTS.md Data-Layer and Student-chat notes and the README.md permission-posture bullet were already synced to this cycle's `messages` tightening (`messageParticipant` link, anti-spoof create, open reads, schema-then-perms push), and no non-cycle docs (CONTEXT.md, docs/SPEC.md, docs/adr/*) reference the message permission posture; REFLECTION.md surfaced one sharp edge that was validation-skipped with no product-doc-mappable body.
