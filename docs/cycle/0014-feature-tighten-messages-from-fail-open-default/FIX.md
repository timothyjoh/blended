## Summary

Cycle 0014's MUST-FIX listed three tasks. **Task 3 (Minor)** was the only one warranting an in-repo change and is **fixed**: BUILD.md line 20 was reworded from the inaccurate "`perms.ts` is outside the coverage include set" (it is matched by the `src/lib/**/*.ts` include at `vitest.config.ts:17`) to the accurate "`perms.ts` is a declarative const object with no executable branches, so it contributes nothing to the v8 line/branch report; its semantics are pinned by the `perms.test.ts` structural guard." Its Verify check passes — `grep -n "outside the coverage include" BUILD.md` returns nothing (exit 1) and the replacement sentence is present.

**Task 1 (Critical, Unmet SPEC AC)** and **Task 2 (Critical, unverified live create rule)** could **not** be fixed in this autonomous environment, for the same reason BUILD.md's "Deviations" already recorded for the build step. Both are operator/live-infrastructure steps, not code changes: Task 1 requires `npx instant-cli push schema` + `npm run perms:push` against the live Instant app, and Task 2 requires a real student sign-in over `/s/:joinCode` against that already-pushed live app to observe whether the create rule's `data.ref`-on-create resolves. The classifier denied both prerequisites here — inspecting `instant-cli` credentials was denied as credential exploration, and a direct `npm run perms:push` was denied as "modifying shared infrastructure permissions." The MUST-FIX itself scopes Task 1 as "(operator step — no code change unless the contingency in Task 2 is needed)," and Task 2's contingency (switching `scalarMatchesLink` to the index form or adding a scalar-anchored author branch) is explicitly conditional on the *live create being rejected* — applying it blind, without the live signal, would be an unverified guess that risks weakening the anti-spoof guarantee, so no speculative rule edit was made. The committed rule, the `messageParticipant` link, and their `perms.test.ts` assertions are ready and in lockstep; the live push and live send remain for an authenticated operator.

**Final test suite:** `npm test` → **271 passed, 8 files, 0 failed**. **Coverage** (`npm run test:coverage`): **Statements 86.42% / Branches 82.10% / Functions 79.59% / Lines 88.75%** — identical to BUILD.md's pre-fix baseline (86.42 / 82.1 / 79.59 / 88.75), no decrease, as expected since the only change was a documentation edit with no code-path impact (per-file: `sessions.ts` 95.09/87.75/85.18/97.2, `db.ts` 91.66/80.39/100/92.59). No new test was required: Task 3 is documentation-only, and Tasks 1–2 introduced no code change. The two critical items remain blocked on live operator execution, not on in-repo work.
